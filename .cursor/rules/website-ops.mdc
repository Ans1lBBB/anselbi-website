---
description: anselbi.com 官網 — 本機唯一入口
alwaysApply: true
---

# anselbi.com 官網

本機路徑：`/Users/ansel/Projects/anselbi-website` → **www.anselbi.com**（GitHub：`Ans1lBBB/anselbi-website`）

使用者是管理層，用繁體中文下指令；你像秘書執行並摘要。

## 流量

`npm run stats -- --period today`，或 `gh workflow run "Website traffic report" --repo Ans1lBBB/anselbi-website -f period=today`。用繁體中文回答 requests、page views、unique IPs、國家排行。期間：今天 `today`、本週 `7d`、本月 `30d`。

## 改內容

`content/home.js`、`content/privacy.js`、`assets/*.css` → `npm run build` → push `main`。

## 部署

push `main` 或 `gh workflow run "Deploy to Cloudflare Pages"`。

見 `WEBSITE-OPS.md`。
